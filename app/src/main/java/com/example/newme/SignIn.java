package com.example.newme;

import android.os.Bundle;

public class SignIn {
    //if user enters the correct pin then display the profile page... If user is not in the database, make sure to not allow sign in
    protected void onCreate(Bundle savedInstanceState) {
        
    }
}
